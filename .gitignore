# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
.env

# Large Models (GitHub Limit 100MB)
models/knn.pkl
models/random_forest.pkl
models/svm.pkl
plots/
processed_data/
reports/
results/
final_reports/

# Reports (Optional, but usually we don't commit generated reports if they are large artifacts, keeping them for now except huge ones)
# Just strict exclusions requested:
